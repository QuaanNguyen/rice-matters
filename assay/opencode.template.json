{
  "$schema": "https://opencode.ai/config.json",

  "//": [
    "Point plugins.package at this clone's packages/opencode-plugin.",
    "demo/reset.js writes a ready copy into demo/work/project/opencode.json.",
    "Permissions are allow-all on purpose: ASSAY is the control, via the plugin's",
    "permission hook. A configured deny would skip the hook."
  ],

  "plugins": [
    {
      "package": "../packages/opencode-plugin",
      "options": {
        "protocol": "../demo/protocol.json"
      }
    }
  ],

  "permission": {
    "read": "allow",
    "edit": "allow",
    "bash": "allow",
    "webfetch": "allow"
  },

  "permissions": [
    { "action": "*", "resource": "*", "effect": "allow" }
  ]
}
